---
description: Cursor远程控制项目：基于Redis的手机远程控制Cursor解决方案
globs: server/*.js, client/*.js, scripts/*.scpt
alwaysApply: false
---

# Cursor远程控制项目规则

本项目实现了一个基于Redis的远程控制Cursor的解决方案，允许用户通过手机发送命令，并通过AppleScript控制Cursor执行。

## 技术栈

- **服务器端**：Node.js + Redis客户端
- **客户端**：基于Web的简单界面（HTML/CSS/JavaScript）
- **自动化**：AppleScript
- **通信**：Redis Pub/Sub

## 项目组件

### 服务器组件

服务器端运行在用户的Mac电脑上，主要负责：

1. 订阅Redis频道，接收来自手机的命令
2. 通过AppleScript控制Cursor执行命令
3. 获取Cursor的响应结果
4. 将结果发布回Redis，供手机客户端获取

```javascript
// 服务器端Redis客户端配置示例
const redisConfig = {
  host: 'your-redis-host.com',
  port: 6379,
  password: 'your-redis-password'
};
```

### AppleScript组件

AppleScript脚本用于模拟用户操作Cursor，包括：

1. 激活Cursor应用
2. 定位到输入框
3. 输入用户命令
4. 等待Cursor的AI响应
5. 提取并返回响应结果

```applescript
-- AppleScript示例：激活Cursor并输入文本
tell application "Cursor" to activate
delay 1
tell application "System Events"
    tell process "Cursor"
        -- 在此处添加具体的UI操作
    end tell
end tell
```

### 客户端组件

客户端是一个简单的Web应用，提供：

1. 命令输入界面
2. 历史记录显示
3. 响应结果展示

## 数据流

1. **命令发送流程**：
   - 手机客户端 → Redis发布 → 服务器订阅 → AppleScript执行 → Cursor处理
  
2. **结果返回流程**：
   - Cursor响应 → AppleScript捕获 → 服务器处理 → Redis发布 → 手机客户端展示

## 安全注意事项

1. 使用适当的Redis身份验证
2. 考虑对命令进行加密或验证
3. 限制Redis访问范围，避免暴露在公网

## 常见问题

1. **Cursor界面变化**：如果Cursor界面更新，可能需要调整AppleScript脚本
2. **响应捕获**：根据Cursor的响应方式调整捕获逻辑
3. **Redis连接**：确保Redis服务可靠，考虑设置重连机制

## 开发指南

开发新功能时，请遵循以下原则：

1. 保持代码模块化，便于维护
2. 为AppleScript添加详细注释
3. 实现错误处理和日志记录
4. 提供用户友好的状态反馈 